stages:
  - deploy

deploy plugins:
  stage: deploy
  tags:
    - studio
  only:
    - master
    - merge_request
  script:
    - for f in `ls`; do rm -rf /var/www/ood/apps/sys/$f; cp $f /var/www/ood/apps/sys/; done

